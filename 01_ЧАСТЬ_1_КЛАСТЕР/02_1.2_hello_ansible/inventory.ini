; ХЕЛЛО-ИНВЕНТАРЬ. Одна «нода» — этот же мак, без SSH (connection=local).
; В бою здесь IP машин — ровно так же устроен inventory Kubespray.
[night_office]
localhost ansible_connection=local

; Группа = роль машин. У Kubespray: kube_control_plane / kube_node / etcd.
