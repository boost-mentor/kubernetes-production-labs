# ШАБЛОН секретов — ЭТОТ файл в git лежит (в нём нет настоящих значений).
# Реальный secrets.yml рядом — зашифрован ansible-vault и/или в .gitignore.
ns_admin_password: "ЗАМЕНИ_МЕНЯ"
ns_api_token: "ЗАМЕНИ_МЕНЯ"
