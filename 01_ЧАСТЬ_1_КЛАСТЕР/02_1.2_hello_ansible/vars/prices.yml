# Обычные (НЕ секретные) переменные — прайс DEVOPS MAY CRY на ночные выезды.
# Меняем ЛЮБУЮ цифру → prays.txt меняется → handler рассылает новый тариф дежурным.
# Порядок ключей = порядок строк в прайсе: dict в Jinja обходится в порядке файла.
call_prices:
  "401 · бабайка под кроватью": 3000
  "402 · шорохи на чердаке": 2500
  "403 · свет мигает в подъезде": 1500
  "404 · вой в вентиляции": 4000
