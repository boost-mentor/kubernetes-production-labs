apiVersion: v1
kind: Service
metadata:
  name: night-shift
  namespace: traffic-lab
  labels:
    app: night-shift
    app.kubernetes.io/name: night-shift
spec:
  type: ClusterIP
  selector:
    app: night-shift
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
